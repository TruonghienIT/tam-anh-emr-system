<Window x:Class="TamAnh_EMR_System.View.Doctor.RescheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dời lịch" Height="200" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Ngày:" VerticalAlignment="Center" Width="60"/>
            <DatePicker x:Name="Dp" SelectedDate="{Binding SelectedDate}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Giờ:" VerticalAlignment="Center" Width="60"/>
            <TextBox x:Name="TbTime" Width="120" />
            <TextBlock Text="(HH:mm)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="#6B7280"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button Width="90" Margin="0,0,8,0" Click="Cancel_Click">Hủy</Button>
            <Button Width="90" Click="Ok_Click">Lưu</Button>
        </StackPanel>
    </Grid>
</Window>
