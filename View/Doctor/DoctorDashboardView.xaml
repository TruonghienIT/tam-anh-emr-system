<UserControl x:Class="TamAnh_EMR_System.View.Doctor.DoctorDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="#F8F9FC" FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="B" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.9"/></Trigger></ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,20,28,28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>

        <!-- ===== LEFT MAIN CONTENT ===== -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>

            <!-- HEADER -->
                <Grid Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="Trang chủ" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="Đây là tổng quan cho ngày hôm nay, ngày 24 tháng 10." FontSize="14" Foreground="#6B7280" Margin="0,6,0,0"/>
                    </StackPanel>
                    <Button Style="{StaticResource B}" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Padding="16,8" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="Calendar" Width="14" Height="14" Foreground="#3B82F6" Margin="0,0,6,0"/>
                            <TextBlock Text="Lịch" FontSize="13" Foreground="#374151" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- STATISTICS CARDS -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

                <!-- Bệnh nhân hôm nay (line chart) -->
                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1">
                    <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/></Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="BỆNH NHÂN HÔM NAY" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                            <Border Width="36" Height="36" CornerRadius="8" Background="#EFF6FF" HorizontalAlignment="Right">
                                <fa:IconImage Icon="UserGroup" Width="16" Height="16" Foreground="#3B82F6"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding PatientsTodayCount}" FontSize="32" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,12"/>
                        <!-- Simplified line chart using polyline -->
                        <Border Height="50" Margin="0,0,0,10" ClipToBounds="True">
                            <Canvas>
                                <Polyline Points="0,40 30,32 60,35 90,20 120,12 150,15 180,8 210,0"
                                          Stroke="#3B82F6" StrokeThickness="2.5" Fill="Transparent"/>
                                <Polyline Points="0,40 30,32 60,35 90,20 120,12 150,15 180,8 210,0 210,50 0,50"
                                          Stroke="Transparent" StrokeThickness="0">
                                    <Polyline.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#333B82F6" Offset="0"/>
                                            <GradientStop Color="#053B82F6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Polyline.Fill>
                                </Polyline>
                            </Canvas>
                        </Border>
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Icon="ArrowTrendUp" Width="12" Height="12" Foreground="#10B981" Margin="0,0,4,0"/>
                            <TextBlock Text="+12%" FontSize="12" Foreground="#10B981" FontWeight="SemiBold"/>
                            <TextBlock Text="  so với hôm trước" FontSize="12" Foreground="#9CA3AF"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Thống kê bệnh (bar chart) -->
                <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1">
                    <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/></Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="THỐNG KÊ BỆNH" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                            <Border Width="36" Height="36" CornerRadius="8" Background="#F3F4F6" HorizontalAlignment="Right">
                                <fa:IconImage Icon="ChartColumn" Width="16" Height="16" Foreground="#6B7280"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding DiseaseStatsCount}" FontSize="32" FontWeight="Bold" Foreground="#111827"/>
                        <TextBlock Text="Trường hợp ghi nhận" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,12"/>
                        <!-- Bar chart -->
                        <ItemsControl ItemsSource="{Binding ChartBars}" Height="60">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="24" CornerRadius="3,3,0,0" VerticalAlignment="Bottom" Height="{Binding BarHeight}" Margin="4,0">
                                        <Border.Background><SolidColorBrush Color="#3B82F6"/></Border.Background>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- APPOINTMENT TABLE -->
            <Border Background="White" CornerRadius="12" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1">
                <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/></Border.Effect>
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="Lịch hẹn hôm nay" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                        <Button Style="{StaticResource B}" Background="Transparent" Padding="0" BorderThickness="0" HorizontalAlignment="Right" Command="{Binding ViewAllCommand}">
                            <TextBlock Text="XEM TẤT CẢ" FontSize="11" FontWeight="Bold" Foreground="#3B82F6"/>
                        </Button>
                    </Grid>
                    <!-- Column headers -->
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,8,0,12">
                        <Grid><Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="1.3*"/><ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="GIỜ" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="1" Text="TÊN BỆNH NHÂN" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="2" Text="BÁC SĨ" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="3" Text="TRẠNG THÁI" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="4" Text="HÀNH ĐỘNG" FontSize="11" FontWeight="Bold" Foreground="#9CA3AF"/>
                        </Grid>
                    </Border>
                    <!-- Rows -->
                    <ItemsControl ItemsSource="{Binding Appointments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,12">
                                    <Grid><Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.2*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="1.3*"/><ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Time}" FontSize="13" FontWeight="SemiBold" Foreground="#111827" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding PatientName}" FontSize="13" Foreground="#374151" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding DoctorName}" FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"/>
                                    <!-- Status badge -->
                                    <Border Grid.Column="3" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FFF7ED"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Đang chờ"><Setter Property="Background" Value="#FFF7ED"/></DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Đã khám"><Setter Property="Background" Value="#ECFDF5"/></DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Khẩn cấp"><Setter Property="Background" Value="#FEF2F2"/></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Đang chờ"><Setter Property="Foreground" Value="#F59E0B"/></DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Đã khám"><Setter Property="Foreground" Value="#10B981"/></DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Khẩn cấp"><Setter Property="Foreground" Value="#EF4444"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <Button Grid.Column="4" Style="{StaticResource B}" Background="Transparent" Padding="0" BorderThickness="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <fa:IconImage Icon="EllipsisVertical" Width="14" Height="14" Foreground="#9CA3AF"/>
                                    </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
        </ScrollViewer>

        <!-- ===== RIGHT PANEL: QUEUE ===== -->
        <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1" VerticalAlignment="Top">
            <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/></Border.Effect>
            <StackPanel>
                <!-- Queue header -->
                <Grid Margin="0,0,0,18">
                    <TextBlock Text="Hàng đợi" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                    <Border Width="28" Height="28" CornerRadius="14" Background="#3B82F6" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding QueueCount}" FontSize="12" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <!-- Queue patients -->
                <ItemsControl ItemsSource="{Binding QueuePatients}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8" CornerRadius="10" Padding="12,10">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#F9FAFB"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUrgent}" Value="True">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                                <Setter Property="BorderBrush" Value="#EF4444"/>
                                                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="40" Height="40" CornerRadius="20" Margin="0,0,10,0">
                                        <Border.Background><SolidColorBrush Color="{Binding AvatarColor}"/></Border.Background>
                                        <TextBlock Text="{Binding Initials}" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="#111827"/>
                                            <!-- Urgent icon -->
                                            <TextBlock Text=" ❗" FontSize="12" Foreground="#EF4444" VerticalAlignment="Center">
                                                <TextBlock.Style><Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers><DataTrigger Binding="{Binding IsUrgent}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                                </Style></TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Text="{Binding WaitingTime}" FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Style="{StaticResource B}" Background="Transparent" Padding="4" BorderThickness="0"
                                            Command="{Binding DataContext.CallPatientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Phone" Width="16" Height="16" Foreground="#3B82F6"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- View all queue link -->
                <Button Style="{StaticResource B}" Background="Transparent" Padding="0,12,0,0" BorderThickness="0" HorizontalAlignment="Center" Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="XEM ĐẦY ĐỦ HÀNG ĐỢI" FontSize="11" FontWeight="Bold" Foreground="#3B82F6"/>
                        <TextBlock Text=" ↗" FontSize="13" Foreground="#3B82F6"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
