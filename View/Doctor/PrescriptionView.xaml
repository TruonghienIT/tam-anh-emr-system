<UserControl x:Class="TamAnh_EMR_System.View.Doctor.PrescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="#F8F9FC"
             FontFamily="Segoe UI"
             IsVisibleChanged="UserControl_IsVisibleChanged">
    <UserControl.Resources>
        <Style x:Key="B" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/><Setter Property="Template"><Setter.Value>
                <ControlTemplate TargetType="Button"><Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.9"/></Trigger></ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="SI" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/><Setter Property="Padding" Value="36,10,14,10"/><Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox">
                <Border x:Name="Bx" Background="White" CornerRadius="8" BorderBrush="#D1D5DB" BorderThickness="1" Padding="{TemplateBinding Padding}">
                    <Grid><ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                    <TextBlock x:Name="P" Text="{TemplateBinding Tag}" Foreground="#9CA3AF" FontSize="13" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/></Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="Text" Value=""><Setter TargetName="P" Property="Visibility" Value="Visible"/></Trigger>
                    <Trigger Property="Text" Value="{x:Null}"><Setter TargetName="P" Property="Visibility" Value="Visible"/></Trigger>
                    <Trigger Property="IsFocused" Value="True"><Setter TargetName="Bx" Property="BorderBrush" Value="#3B82F6"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24,20,24,24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Border CornerRadius="14" Padding="24,20" Margin="0,0,0,20">
                <Border.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5"><GradientStop Color="#EFF6FF" Offset="0"/><GradientStop Color="#DBEAFE" Offset="1"/></LinearGradientBrush></Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tra Cứu Bệnh Nhân" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,4"/>
                        <TextBlock Text="Tìm kiếm nhanh hoặc thêm hồ sơ mới" FontSize="12" Foreground="#64748B" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="40" Height="40" CornerRadius="10" Background="White" Margin="0,0,10,0">
                            <fa:IconImage Icon="ChartColumn" Width="18" Height="18" Foreground="#3B82F6"/>
                        </Border>
                        <Button Style="{StaticResource B}" Background="White" BorderThickness="0" Padding="14,10" Command="{Binding AddPatientCommand}">
                            <StackPanel HorizontalAlignment="Center">
                                <fa:IconImage Icon="UserPlus" Width="18" Height="18" Foreground="#3B82F6" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                <TextBlock Text="Thêm Bệnh&#x0a;Nhân" FontSize="11" Foreground="#3B82F6" FontWeight="SemiBold" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,28">
                <TextBox Style="{StaticResource SI}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Tag="Nhập tên, số điện thoại hoặc mã BN..."/>
                <fa:IconImage Icon="MagnifyingGlass" Width="15" Height="15" Foreground="#9CA3AF" HorizontalAlignment="Left" Margin="12,0,0,0" IsHitTestVisible="False"/>
            </Grid>

            <Border Background="White" CornerRadius="14" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1">
                <Border.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Danh Sách Toa Thuốc" FontSize="18" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,16"/>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,8,0,10">
                        <Grid><Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.3*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Ngày Khám" FontSize="11" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="1" Text="Bệnh Nhân" FontSize="11" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="2" Text="Bác Sĩ Điều Trị" FontSize="11" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                        <TextBlock Grid.Column="3" Text="Trạng Thái" FontSize="11" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                        </Grid>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="420">
                    <ItemsControl ItemsSource="{Binding Prescriptions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.SelectPrescriptionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Cursor="Hand">
                                    <Button.Template><ControlTemplate TargetType="Button">
                                        <Border x:Name="Row" BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,12" Background="Transparent">
                                            <Grid><Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1.3*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.2*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DateDisplay}" FontSize="13" FontWeight="SemiBold" Foreground="#111827"/>
                                                <TextBlock Text="{Binding TimeDisplay}" FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding PatientName}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="2" Text="{Binding DoctorName}" FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"/>
                                            <Border Grid.Column="3" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="#EFF6FF"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Mới kê"><Setter Property="Background" Value="#EFF6FF"/></DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Đã nhận"><Setter Property="Background" Value="#ECFDF5"/></DataTrigger>
                                                    </Style.Triggers></Style></Border.Style>
                                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold">
                                                    <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="#3B82F6"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Mới kê"><Setter Property="Foreground" Value="#3B82F6"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đã nhận"><Setter Property="Foreground" Value="#10B981"/></DataTrigger>
                                                        </Style.Triggers></Style></TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Row" Property="Background" Value="#F9FAFB"/></Trigger>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter TargetName="Row" Property="Background" Value="#EFF6FF"/></DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate></Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Column="2" Background="White" CornerRadius="14" Padding="20,18" BorderBrush="#E5E7EB" BorderThickness="1" VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.06"/>
            </Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="Chi Tiết Toa Thuốc" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                    <Button Style="{StaticResource B}" Background="Transparent" Padding="2" BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <fa:IconImage Icon="EllipsisVertical" Width="14" Height="14" Foreground="#9CA3AF"/>
                    </Button>
                </Grid>
                <Border Background="#F9FAFB" CornerRadius="10" Padding="14,12" Margin="0,0,0,16" BorderBrush="#E5E7EB" BorderThickness="1">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedPrescription.PatientName}" FontSize="15" FontWeight="Bold" Foreground="#111827"/>
                            <TextBlock FontSize="12" Foreground="#6B7280" Margin="0,3,0,0">
                                <Run Text="ID: "/><Run Text="{Binding SelectedPrescription.PatientId}"/>
                            </TextBlock>
                        </StackPanel>
                        <TextBlock Text="{Binding SelectedPrescription.DateDisplay}" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Grid>
                </Border>
                <ItemsControl ItemsSource="{Binding SelectedPrescriptionDetails}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="10" Padding="16,14" Margin="0,0,0,10" BorderBrush="#E5E7EB" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="#111827"/>
                                        <Border CornerRadius="10" Padding="8,3" HorizontalAlignment="Right" Background="#FEF3C7">
                                            <TextBlock Text="{Binding QuantityDisplay}" FontSize="11" FontWeight="Bold" Foreground="#D97706"/>
                                        </Border>
                                    </Grid>
                                    <TextBlock Text="{Binding Dosage}" FontSize="13" Foreground="#374151" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding Instruction}" FontSize="12" Foreground="#9CA3AF"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Style="{StaticResource B}" Padding="0,14" Margin="0,8,0,0" Command="{Binding PrintCommand}" HorizontalAlignment="Stretch">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#3B82F6" Offset="0"/>
                            <GradientStop Color="#2563EB" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <fa:IconImage Icon="Print" Width="16" Height="16" Foreground="White" Margin="0,0,8,0"/>
                        <TextBlock Text="In đơn thuốc" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
