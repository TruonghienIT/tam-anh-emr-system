<Window x:Class="TamAnh_EMR_System.View.Receptionist.RegisterPatientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:view="clr-namespace:TamAnh_EMR_System.View"
        Title="Thêm bệnh nhân mới" Height="750" Width="1100"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Border Background="White" CornerRadius="12" Margin="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.15" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header bar for dragging and closing -->
            <Border Background="#F9FAFB" CornerRadius="12,12,0,0" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" MouseLeftButtonDown="Border_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <fa:IconImage Icon="UserPlus" Width="16" Height="16" Foreground="#3B82F6" Margin="0,0,8,0"/>
                        <TextBlock Text="ĐĂNG KÝ BỆNH NHÂN MỚI" FontSize="14" FontWeight="Bold" Foreground="#111827"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0"
                            Click="CloseButton_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" Background="Transparent" CornerRadius="4" Padding="6">
                                    <fa:IconImage x:Name="icon" Icon="Xmark" Width="16" Height="16" Foreground="#6B7280"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#FEE2E2"/>
                                        <Setter TargetName="icon" Property="Foreground" Value="#EF4444"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Body -->
            <Border Grid.Row="1" CornerRadius="0,0,12,12" ClipToBounds="True">
                <view:RegisterPatientView Margin="0"/>
            </Border>
        </Grid>
    </Border>
</Window>
